name = "recon_specialist"
category = "Recon"
description = "Network and wireless reconnaissance work. Site mapping, signal characterization, environment fingerprinting, and target identification."

default_daemon_addendum = """
When this profile is active, prefer scripts over fresh tool composition.
Use SHRDLU-style targeting language: name what you want done, name the
target, name the desired output shape. Recon work rewards precision over
exploration — if the netrunner's goal could match multiple targets, ask
before spawning.
"""

default_construct_addendum = """
You are operating in a recon context. Your job is to characterize what
exists, not to act on it. Read-only is the default posture; if you find
yourself wanting to write or modify, stop and ask. Findings should be
structured: targets identified, signals observed, gaps remaining.
"""

# Registry-backed tools for recon work. References entries in
# <home>/tools/tools.toml. Empty default — register recon CLIs
# (nmap, subfinder, dnsx, naabu, ripgrep, etc.) in tools.toml and
# add their names here to surface them in the construct's prompt
# addendum. Claude Code's built-in Bash/Read/Glob/Grep/WebSearch/
# WebFetch are always available regardless. P4 of the
# tools/plugins/profiles retool (2026-05-03) renamed
# `recommended_tools` → `tools`.
tools = []
